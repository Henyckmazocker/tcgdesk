<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>

        <!--
            Los tests que hablan con MySQL de verdad. Van en su propia suite
            porque no prueban lo mismo: una consulta solo falla contra datos, y
            estos necesitan el catálogo cargado. `composer test` corre las dos;
            `composer test:unit` corre solo la de arriba, que no necesita nada.
            Si no hay base de datos se saltan solos, nunca se ponen rojos.
        -->
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
        <exclude>
            <directory>src/Infrastructure</directory>
        </exclude>
    </source>
</phpunit>
